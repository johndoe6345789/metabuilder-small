'use client';

// Re-export FormDialog from @metabuilder/components for backward compatibility
export {
  FormDialog as default,
  type FormDialogProps,
  type FormField,
} from '@metabuilder/fakemui';
