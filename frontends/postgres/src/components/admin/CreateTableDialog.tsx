'use client';

// Re-export CreateTableDialog from @metabuilder/components for backward compatibility
export {
  CreateTableDialog as default,
  type CreateTableDialogProps,
  type TableColumn,
} from '@metabuilder/fakemui';
