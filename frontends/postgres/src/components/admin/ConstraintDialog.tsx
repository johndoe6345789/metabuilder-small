'use client';

// Re-export ConstraintDialog from @metabuilder/components for backward compatibility
export {
  ConstraintDialog as default,
  type ConstraintDialogProps,
  type ConstraintDialogMode,
  type ConstraintType,
  type ConstraintInfo,
} from '@metabuilder/fakemui';
