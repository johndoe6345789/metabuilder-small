'use client';

// Re-export DataGrid from @metabuilder/components for backward compatibility
export {
  DatabaseDataGrid as default,
  type DatabaseDataGridProps as DataGridProps,
  type DataGridColumn,
} from '@metabuilder/fakemui';
