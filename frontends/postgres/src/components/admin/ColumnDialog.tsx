'use client';

// Re-export ColumnDialog from @metabuilder/components for backward compatibility
export {
  ColumnDialog as default,
  type ColumnDialogProps,
  type ColumnDialogMode,
  type ColumnInfo,
} from '@metabuilder/fakemui';
