/**
 * TranslationEditor Interface
 *
 * Props for the translation editor JSON component.
 * This widget is self-contained — it manages its own state via useTranslationEditor.
 */
export interface TranslationEditorProps {
  /* No external props needed — all state is managed internally via the hook */
}
