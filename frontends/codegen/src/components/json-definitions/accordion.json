{
  "id": "accordion-container",
  "type": "div",
  "bindings": {
    "className": {
      "source": "className",
      "transform": "const base = 'accordion'; return data ? `${base} ${data}` : base"
    }
  },
  "children": [
    {
      "id": "accordion-items-repeat",
      "type": "_repeat",
      "bindings": {
        "_items": {
          "source": "items",
          "transform": "data || []"
        },
        "_renderItem": {
          "source": ["accordionState.toggleItem", "accordionState.isOpen"],
          "transform": "(item) => { const toggleItem = data[0]; const isOpen = data[1]; const itemIsOpen = isOpen(item.id); return { _element: 'div', _key: item.id, _props: { className: `accordion__item${item.disabled ? ' accordion__item--disabled' : ''}` }, _children: [{ _element: 'button', _props: { onClick: () => !item.disabled && toggleItem(item.id), disabled: item.disabled, className: `accordion__trigger${itemIsOpen ? ' accordion__trigger--open' : ''}` }, _children: [{ _element: 'span', _children: [item.title] }, { _element: 'CaretDown', _props: { className: `accordion__trigger-icon${itemIsOpen ? ' accordion__trigger-icon--open' : ''}` } }] }, itemIsOpen ? { _element: 'div', _props: { className: 'accordion__content' }, _children: [item.content] } : null].filter(Boolean) }; }"
        }
      }
    }
  ]
}
