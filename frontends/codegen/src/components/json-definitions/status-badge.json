{
  "id": "status-badge-root",
  "type": "Badge",
  "bindings": {
    "className": {
      "source": "status",
      "transform": "(() => { const status = data || 'inactive'; const statusMap = { active: 'status-badge--active', inactive: 'status-badge--inactive', pending: 'status-badge--pending', error: 'status-badge--error', primary: 'status-badge--primary' }; const modifierClass = statusMap[status] || 'status-badge--inactive'; return ('status-badge ' + modifierClass).trim(); })()"
    },
    "children": "children"
  }
}
