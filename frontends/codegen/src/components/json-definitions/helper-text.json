{
  "id": "helper-text",
  "type": "p",
  "bindings": {
    "className": {
      "source": ["variant", "className"],
      "transform": "const variant = data[0] || 'default'; const extra = data[1] || ''; const variantMods = { default: '', error: ' helper-text--error', success: ' helper-text--success' }; const mod = variantMods[variant] || ''; return ('helper-text' + mod + (extra ? ' ' + extra : '')).trim()"
    },
    "children": {
      "source": "children",
      "transform": "data"
    }
  }
}
