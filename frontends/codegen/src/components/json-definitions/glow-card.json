{
  "id": "glow-card",
  "type": "Card",
  "bindings": {
    "onClick": {
      "source": "onClick",
      "transform": "data"
    },
    "className": {
      "source": ["glowColor", "intensity", "className", "onClick"],
      "transform": "const glowColor = data[0] || 'primary'; const intensity = data[1] || 'medium'; const extra = data[2] || ''; const hasClick = data[3]; const colorClass = { primary: 'glow-card--primary', accent: 'glow-card--accent', success: 'glow-card--success', warning: 'glow-card--warning', error: 'glow-card--error' }[glowColor] || 'glow-card--primary'; const intensityClass = { low: 'glow-card--low', medium: '', high: 'glow-card--high' }[intensity] || ''; const clickClass = hasClick ? 'glow-card--clickable' : ''; return ['glow-card', colorClass, intensityClass, clickClass, extra].filter(Boolean).join(' ')"
    }
  },
  "children": {
    "source": "children",
    "transform": "data"
  }
}
