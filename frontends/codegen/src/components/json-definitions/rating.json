{
  "id": "rating-root",
  "type": "div",
  "bindings": {
    "className": {
      "source": "className",
      "transform": "data ? 'rating ' + data : 'rating'"
    }
  },
  "children": [
    {
      "id": "rating-stars",
      "type": "div",
      "props": {
        "className": "rating__stars"
      },
      "children": "(() => { const max = max || 5; return Array.from({ length: max }, (_, idx) => ({ id: `star-${idx}`, type: 'button', props: { className: idx < (value || 0) ? 'rating__star rating__star--filled' : 'rating__star', 'data-rating': idx + 1 }, bindings: { onClick: `() => onChange(${idx + 1})` } })); })()"
    },
    {
      "id": "rating-value",
      "type": "span",
      "props": {
        "className": "rating__value"
      },
      "bindings": {
        "children": {
          "source": "value",
          "transform": "data ? `${data.toFixed(1)} / ${max || 5}` : ''"
        }
      },
      "conditional": {
        "if": "showValue"
      }
    }
  ]
}
