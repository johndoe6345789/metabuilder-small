{
  "id": "card",
  "type": "div",
  "bindings": {
    "onClick": {
      "source": "onClick",
      "transform": "data"
    },
    "className": {
      "source": ["variant", "padding", "hover", "onClick", "className"],
      "transform": "const variant = data[0] || 'default'; const padding = data[1] || 'md'; const hover = data[2]; const onClick = data[3]; const extra = data[4] || ''; const variantClass = { default: '', bordered: 'card--bordered', elevated: 'card--elevated', flat: 'card--flat' }[variant] || ''; const paddingClass = { none: 'card--no-padding', sm: 'card--sm', md: 'card--md', lg: 'card--lg' }[padding] || ''; const interactiveClass = (hover || onClick) ? 'card--interactive' : ''; return ['card', variantClass, paddingClass, interactiveClass, extra].filter(Boolean).join(' ')"
    }
  },
  "children": [
    {"type": "slot", "source": "children"}
  ]
}
