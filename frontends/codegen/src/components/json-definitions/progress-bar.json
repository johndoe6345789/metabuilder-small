{
  "id": "progress-bar-wrapper",
  "type": "div",
  "props": {
    "className": "progress-bar"
  },
  "children": [
    {
      "id": "progress-bar-container",
      "type": "div",
      "bindings": {
        "className": {
          "source": ["size", "className"],
          "transform": "const size = data[0] || 'md'; const extra = data[1] || ''; const sizeMod = { sm: ' progress-bar__track--sm', md: ' progress-bar__track--md', lg: ' progress-bar__track--lg' }; return ('progress-bar__track' + (sizeMod[size] || ' progress-bar__track--md') + (extra ? ' ' + extra : '')).trim()"
        },
        "role": {
          "source": null,
          "transform": "'progressbar'"
        },
        "aria-valuenow": {
          "source": "value",
          "transform": "data"
        },
        "aria-valuemin": {
          "source": null,
          "transform": "0"
        },
        "aria-valuemax": {
          "source": "max",
          "transform": "data || 100"
        }
      },
      "children": [
        {
          "id": "progress-bar-fill",
          "type": "div",
          "bindings": {
            "className": {
              "source": "variant",
              "transform": "const mods = { default: '', accent: ' progress-bar__fill--accent', destructive: ' progress-bar__fill--destructive' }; return ('progress-bar__fill' + (mods[data] || '')).trim()"
            },
            "style": {
              "source": ["value", "max"],
              "transform": "const value = data[0] || 0; const max = data[1] || 100; const percentage = Math.min(Math.max((value / max) * 100, 0), 100); return { width: `${percentage}%` }"
            }
          }
        }
      ]
    },
    {
      "id": "progress-bar-label",
      "type": "span",
      "bindings": {
        "className": {
          "source": null,
          "transform": "'progress-bar__label'"
        },
        "children": {
          "source": ["value", "max"],
          "transform": "const value = data[0] || 0; const max = data[1] || 100; const percentage = Math.min(Math.max((value / max) * 100, 0), 100); return `${Math.round(percentage)}%`"
        },
        "_if": {
          "source": "showLabel",
          "transform": "!!data"
        }
      }
    }
  ]
}
