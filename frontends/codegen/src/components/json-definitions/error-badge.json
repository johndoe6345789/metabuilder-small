{
  "id": "error-badge",
  "type": "Badge",
  "bindings": {
    "variant": {
      "source": "variant",
      "transform": "data || 'destructive'"
    },
    "className": {
      "source": ["count", "size"],
      "transform": "(() => { const size = data[1] || 'md'; const sizeClass = size === 'sm' ? 'error-badge--sm' : 'error-badge--md'; return ('error-badge error-badge--destructive ' + sizeClass).trim(); })()"
    },
    "children": {
      "source": "count",
      "transform": "data"
    },
    "_if": {
      "source": "count",
      "transform": "data !== 0"
    }
  }
}
